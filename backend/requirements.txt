# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Backend FastAPI
# ─────────────────────────────────────────────────────────────────────────────

# Server ASGI
fastapi==0.111.0
uvicorn[standard]==0.30.1   # include uvloop + httptools

# Socket.IO
python-socketio==5.11.2     # server Socket.IO async
# websockets rimosso: Socket.IO usa il suo transport layer

# Redis (async, supporto Pub/Sub)
redis[hiredis]==5.0.4       # hiredis = parser C, più veloce

# Validazione dati
pydantic==2.7.1
pydantic-settings==2.3.0    # gestione variabili d'ambiente

# Metriche Prometheus per FastAPI
prometheus-fastapi-instrumentator==7.0.0

# Utility
python-dotenv==1.0.1