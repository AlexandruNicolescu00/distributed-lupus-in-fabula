# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Backend FastAPI
# ─────────────────────────────────────────────────────────────────────────────

# Server ASGI
fastapi==0.111.0
uvicorn[standard]==0.30.1   # include uvloop + websockets + httptools
websockets==12.0

# Redis (async, supporto Pub/Sub)
redis[hiredis]==5.0.4       # hiredis = parser C, più veloce

# Validazione dati (già incluso in FastAPI ma esplicito per chiarezza)
pydantic==2.7.1
pydantic-settings==2.3.0    # gestione variabili d'ambiente

# Metriche Prometheus per FastAPI
prometheus-fastapi-instrumentator==7.0.0

# Utility
python-dotenv==1.0.1